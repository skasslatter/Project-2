<div class="container">
  <div class="row justify-content-center">
    <div class="card mb-3">
      <img src="{{recipe.imgPath}}" class="card-img-top detail" alt="Recipe">
      <div class="card-body" id="recipe">
        <h5 class="card-title detail-title">{{recipe.title}}</h5>
        <ul>
          <li>duration: {{recipe.duration}} min.</li>
        </ul>

        <div>
          <h6>Available ingredients</h6>
          <ul>
            {{#each availableIngredients}}
            <li>{{this.name}}</li>
            {{/each}}
          </ul>
        </div>

        <div>
          <h6>Missing ingredients</h6>
          <ul>
            {{#each missingIngredients}}
            <li>{{this.name}}</li>
            {{/each}}
          </ul>
        </div>

        <p>{{recipe.description}}</p>

        <p id="recipeId" hidden>{{recipe.id}}</p>
      </div>
      <div class="card-body">

        <div class="row justify-content-around flex-column d-flex align-items-center">
          <a href="/shoppinglist/{{recipe.id}}" class="btn btn-success col-md-3 ">I want to make this recipe!</a>
          <a href="javascript:window.print()" class="btn btn-success col-md-3">Download recipe</a>
          <a href="/fridge" id="favourite" class="btn btn-success col-md-3">Add to favourites</a>
          {{!-- <a href="/recipes/{{recipe.id}}/update" class="btn btn-warning col-md-5 ">Update recipe</a> --}}
          {{!-- <a href="/recipes/{{recipe.id}}/delete" class="btn btn-danger col-md-5">Delete recipe</a> --}}
          {{!-- change href --}}
        </div>
      </div>
    </div>
  </div>
</div>

